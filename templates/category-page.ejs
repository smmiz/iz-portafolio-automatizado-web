<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="./style.css">
</head>
<body>
    <div class="container">
        <header>
            <a href="./index.html" class="back-link">&larr; Volver al Inicio</a>
            <h1><%= title %></h1>
        </header>
        <main class="gallery">
            <% models.forEach(model => { %>
                <div class="model-card">
                    <a href="./models/<%= model.gender %>/<%= model.modelSlug %>.html">

                        <img src="<%= model.coverImageUrl %>" alt="Portada de <%= model.fullName %>" class="cover-image">

                        <h3><%= model.fullName %></h3>
                    </a>
                </div>
            <% }); %>
        </main>
    </div>
</body>
</html>